if (session.quic)
  div.sessionDetailMeta.bold quic
  dl.sessionDetailMeta
    +arrayList(session.quic, "host", "Hosts", "host.quic")
    +arrayList(session.quic, "useragent", "User-Agents", "quic.user-agent")
    +arrayList(session.quic, "version", "Versions", "quic.version")
