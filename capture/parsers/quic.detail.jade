if (session.quic)
  div.sessionDetailMeta.bold quic
  dl.sessionDetailMeta
    +stringList(session.quic, "host-term", "Hosts", "host.quic")
    +stringList(session.quic, "ua-term", "User-Agents", "quic.user-agent")
    +stringList(session.quic, "version-term", "Versions", "quic.version")
