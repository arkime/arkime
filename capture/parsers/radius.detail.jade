if (session.radius)
  div.sessionDetailMeta.bold radius
  dl.sessionDetailMeta
    +stringList(session.radius, "user-term", "User", "radius.user")
    +stringList(session.radius, "mac-term", "Mac", "radius.mac")
    +ipArrayList(session.radius, "eip", "Endpoint IPs", "radius.endpoint-ip")
    +ipArrayList(session.radius, "fip", "Framed IPs", "radius.framed-ip")
