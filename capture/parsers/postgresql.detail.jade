if (session.postgresql)
  h4 postgresql
  dl
    +stringList(session.postgresql, "user-term", "Users", "postgresql.user")
    +stringList(session.postgresql, "db-term", "DBs", "postgresql.db")
    +stringList(session.postgresql, "app-term", "Applications", "postgresql.app")
