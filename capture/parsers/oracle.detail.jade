if (session.oracle)
  div.sessionDetailMeta.bold oracle
  dl.sessionDetailMeta
    +stringList(session.oracle, "user-term", "Users", "oracle.user")
    +stringList(session.oracle, "host-term", "Hosts", "oracle.host")
    +stringList(session.oracle, "service-term", "Services", "oracle.service")
