if (session.oracle)
  div.sessionDetailMeta.bold oracle
  dl.sessionDetailMeta
    +arrayList(session.oracle, "user", "Users", "oracle.user")
    +arrayList(session.oracle, "host", "Hosts", "oracle.host")
    +arrayList(session.oracle, "service", "Services", "oracle.service")
