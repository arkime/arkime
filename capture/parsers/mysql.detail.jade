if (session.mysql)
  div.sessionDetailMeta.bold mysql
  dl.sessionDetailMeta
    +arrayList(session.mysql, "user", "Users", "mysql.user")
    +arrayList(session.mysql, "version", "Versions", "mysql.ver")
