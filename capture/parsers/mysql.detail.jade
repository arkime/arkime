if (session.mysql)
  div.sessionDetailMeta.bold mysql
  dl.sessionDetailMeta
    +stringList(session.mysql, "user-term", "Users", "mysql.user")
    +stringList(session.mysql, "ver-term", "Versions", "mysql.ver")
