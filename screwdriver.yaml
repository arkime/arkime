jobs:
    main:
        image: centos:7
        steps:
            - yum install -y sudo perl-Test-Base perl-Test-Differences
            - ./easybutton-build.sh
            - export PATH=/data/moloch/bin:$PATH make install
            - ls -lR /data
            - (cd tests ; ./tests.pl)

